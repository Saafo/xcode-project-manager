import XCTest
//@testable import xcode_project_manager

final class xcode_project_managerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(XcodeProjectManager().text, "Hello, World!")
    }
}
